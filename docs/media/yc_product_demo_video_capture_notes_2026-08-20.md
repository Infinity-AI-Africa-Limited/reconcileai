# YC Product Demo Video — Live Platform Capture Notes

## Live platform inspection

**URL:** `https://www.reconcileaiafrica.com/dashboard`  
**Access:** Logged-in Infinity AI Staff session, inspected 20 August 2026.

The actual platform navigation exposes these relevant real modules: Settlement Monitor, Dashboard, Super Agent, Exception Intelligence, Demo Dashboard, Distributor Registry, Sync Status, SHOPLINE Connection, Upload Data, Reconciliation, Multi-Channel, Payment Exceptions, Age Tracker, Orders & Payments, Review Queue, Audit Trail, Data Protection, CBN Reports, Team Access, Assessments, Module Configuration, and Email Settings.

The currently selected Main View dashboard contains no records in this staff context: 0 transactions, 0% match rate, 0 open exceptions, and 0 jobs. The demo must not present this empty state as traction or attempt to fabricate data. The next capture step is to identify the actual vertical/context switcher and live views with safely displayable, approved, or masked information.

The live Settlement Monitor route is also accessible and correctly renders the actual SHOPLINE retail-commerce workflow: settlement status, pending settlement, match rate, open exceptions, connected stores, payouts, and merchant exception-resolution intelligence. In the current Infinity AI Staff context, it reports no active SHOPLINE stores and no settlement/payout data. This is an authentic empty tenant state and must not be used as the substantive retail-commerce demonstration.

The live Platform Control Centre confirms the product’s deployed vertical architecture: 6 organisations, 26 users, 31 reconciliation jobs, 2 Financial Services organisations, 1 Corporate B2B organisation, and 1 Infinity AI internal organisation. The retail-commerce organisation count currently renders as 0 in this platform-level view. This means the live video can substantively show Financial Services and Corporate B2B tenant workflows. Retail Commerce can be shown through the real Settlement Monitor and SHOPLINE Connection surfaces, but it must be framed as an operating integration surface, not a live merchant transaction case in this staff context.

The organisation list identifies actual staff-accessible tenant portals: BrightGoods Nigeria Ltd (Demo) for Corporate B2B; Globus Bank Nigeria (Demo) and ReconcileAI Guest Demo for Financial Services; and ReconcileAI plus ReconcileAI Dev Store for Retail Commerce. The two retail organisations are active in the portal list. The video may use the real production platform’s portal mode and must include an on-screen/narrated disclosure whenever a tenant is demonstrative or contains synthetic/masked data. The next action is to enter the active ReconcileAI Dev Store portal, verify the real SHOPLINE connection/status views, and capture only non-sensitive evidence.

The browser controller can enter the active ReconcileAI Dev Store portal and exposes authentic retail-commerce metrics (6 total records, 4 matched, 66.7% match rate, 1 open exception, and 2 unmatched records). However, a physical screen capture of the connected computer remains on a different browser tab at the empty staff dashboard, even while the browser controller is operating the authenticated tenant portal. The temporary capture must therefore not be used as final video footage until the intended controlled tab can be made visible locally.

The controlled local Chrome capture now mirrors the browser-controller session. The first live product sequence is the actual ReconcileAI Dev Store Retail Commerce portal: 6 total records across two SHOPLINE channels, 4 matched, 2 unmatched, a 66.7% current view match rate, and one open exception. This is a real development-store tenant view, not an invented application screen. The script must call it a development-store demonstration and must not generalise these values into product-wide performance claims.

The live Settlement Monitor verifies an active `reconcileai-dev` SHOPLINE connection and displays settlement status, pending settlement, connected store state, last sync, exception count, and exception-resolution intelligence. The current values are development-store data and must be labelled synthetic or masked. The actual Orders & Payments route also exposes its real search/filter interface, but currently renders no rows in this tenant context; it should be shown only briefly or omitted from the final edit in favour of the populated Settlement Monitor and Payment Exceptions workflow.

The actual Payment Exceptions route provides the real exception-management controls: date range, status/category/severity filters, export, and glossary. The dashboard’s one open exception is outside the current date views, so this route has no populated record to open safely in the retail tenant. The video should not imply a visible retail exception has been resolved. The populated financial-services tenant will instead provide the unresolved-exception, owner/ageing, AI-assist, human-approval, and audit-trail sequence.

## User-mandated content constraints

1. Record actual platform views rather than mock screens or screenshots.
2. Use a controlled, synthetic, approved, or masked data set and state that choice aloud.
3. Mention the three platform verticals, with emphasis on financial services and retail commerce.
4. Show the operational sequence: ingestion, completed reconciliation, one unresolved exception with owner/ageing, AI-assisted investigation, human approval, and audit evidence.
5. Final asset: maximum 3 minutes and under 100 MB, using a realistic Nigerian male narrator.
