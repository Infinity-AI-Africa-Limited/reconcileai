# ─────────────────────────────────────────────────────────────────────────────
# Ollama Modelfile — ReconcileAI reconciliation analyst (CPU, fine-tuned)
#
# After you fine-tune and quantize (see ../../ml/README.md), you get a file like
# `reconcileai-recon-3b-q4_k_m.gguf`. Place it next to this Modelfile and run:
#
#   ollama create reconcileai -f Modelfile
#
# Then set RECON_MODEL=reconcileai in .env.onprem and restart the CPU stack.
# ─────────────────────────────────────────────────────────────────────────────

FROM ./reconcileai-recon-3b-q4_k_m.gguf

# Deterministic, low-creativity decoding — this is a classification/explanation
# task, not creative writing.
PARAMETER temperature 0.2
PARAMETER top_p 0.9
PARAMETER repeat_penalty 1.05
PARAMETER num_ctx 8192
PARAMETER stop "<|im_end|>"

# Baseline domain framing. The app also sends its own system prompt per call;
# this is the fallback identity baked into the model.
SYSTEM """You are the ReconcileAI reconciliation analyst for Nigerian core banking systems (Apache Fineract / Woodcore CBS). You classify GL-vs-subledger exceptions, explain them in plain English for a finance officer, assign a priority by financial impact, and recommend a specific corrective action. When asked for structured output, reply with strict JSON only."""

# ChatML template (Qwen2.5 family). Adjust if you fine-tune a different base.
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>
"""
